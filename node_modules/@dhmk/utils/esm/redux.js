import { id } from "./std";
export function selector(fa, fb) {
    if (fb)
        return function (s) { return fb(fa(s)); };
    else {
        var ff = function (fb) {
            if (fb === void 0) { fb = id; }
            return function (s) {
                return fb(fa(s));
            };
        };
        ff.refine = function (fb) {
            if (fb === void 0) { fb = id; }
            return selector(function (s) { return fb(fa(s)); });
        };
        return ff;
    }
}
