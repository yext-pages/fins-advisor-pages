"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readPackageJson = void 0;
const file_utils_1 = require("./file.utils");
const readPackageJson = (pathToPackageJson) => __awaiter(void 0, void 0, void 0, function* () {
    const doesPackageJsonExist = yield file_utils_1.doesFileExist(pathToPackageJson);
    if (!doesPackageJsonExist) {
        throw new Error("Cannot find the package.json: " + pathToPackageJson);
    }
    const packageJsonAsString = yield file_utils_1.readFileAsync(pathToPackageJson, { encoding: "utf8" });
    const packageJson = JSON.parse(packageJsonAsString);
    return packageJson;
});
exports.readPackageJson = readPackageJson;
