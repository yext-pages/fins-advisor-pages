import initialStudioData from "virtual:yext-studio";
const createPreviousSaveSlice = (set) => ({
    siteSettings: {
        values: initialStudioData.siteSettings?.values,
    },
    fileMetadatas: {
        UUIDToFileMetadata: initialStudioData.UUIDToFileMetadata,
    },
    setPreviousSave(saveState) {
        set(saveState);
    },
});
export default createPreviousSaveSlice;
//# sourceMappingURL=createPreviousSaveSlice.js.map