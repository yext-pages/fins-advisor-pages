import PageSlice from "../../models/slices/PageSlice";
import { ModuleMetadata } from "@yext/studio-plugin";
/**
 * Creates an action that loops through all pages, and detaches all instances of the given module.
 */
export default function createDetachAllModuleInstances(get: () => PageSlice): (metadata: ModuleMetadata) => void;
//# sourceMappingURL=detachAllModuleInstances.d.ts.map