import { ComponentState, ModuleMetadata } from "@yext/studio-plugin";
import FileMetadataSlice from "../models/slices/FileMetadataSlice";
/**
 * Imports a component into the global store.
 *
 * Modules are not directly imported, but instead have all their constituents
 * imported instead, similar to a Page.
 */
export default class ImportComponentAction {
    private getFileMetadataSlice;
    constructor(getFileMetadataSlice: () => FileMetadataSlice);
    importComponent: (c: ComponentState) => Promise<void>;
    private importStandardOrModuleComponentState;
    importModule: (metadata: ModuleMetadata) => Promise<void[]>;
}
//# sourceMappingURL=ImportComponentAction.d.ts.map