import { ValidFileMetadata } from "@yext/studio-plugin";
import StudioActions from "../StudioActions";
export default class AddComponentAction {
    private studioActions;
    constructor(studioActions: StudioActions);
    /**
     * Adds the component to the current active component tree.
     */
    addComponent: (metadata: ValidFileMetadata) => void;
    /**
     * Inserts the given compomnent state into the tree.
     * Tries to insert as a child of the active component, if possible.
     */
    private insertComponentState;
}
//# sourceMappingURL=AddComponentAction.d.ts.map