import { StudioHMRPayload } from "@yext/studio-plugin";
/**
 * A handler for custom Studio HMR events.
 * When a custom studio HMR event is received, updates the store.
 */
export default function hotReloadStore(payload: StudioHMRPayload): void;
//# sourceMappingURL=hotReloadStore.d.ts.map