import { MessageID, StudioEventMap } from "@yext/studio-plugin";
/**
 * Send a message to the backend, e.g. to save changes to file or deploy changes.
 *
 * @returns A Promise that will be resolved once a response is received from the server.
 */
export default function sendMessage<T extends MessageID>(messageId: T, payload: StudioEventMap[T]): Promise<string>;
//# sourceMappingURL=sendMessage.d.ts.map