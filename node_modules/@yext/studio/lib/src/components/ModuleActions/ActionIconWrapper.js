import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import classNames from "classnames";
import { useMemo } from "react";
import { Tooltip } from "react-tooltip";
import { v4 } from "uuid";
export default function ActionIconWrapper(props) {
    const anchorId = useMemo(() => v4(), []);
    const className = classNames("rounded p-1", {
        "text-gray-400": props.disabled,
        "text-violet-600 hover:bg-slate-200": !props.disabled,
    });
    return (_jsxs("div", { className: className, id: anchorId, children: [props.children, _jsx(Tooltip, { anchorId: anchorId, content: props.tooltip })] }));
}
//# sourceMappingURL=ActionIconWrapper.js.map