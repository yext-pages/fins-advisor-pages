import { jsx as _jsx } from "react/jsx-runtime";
import { ComponentStateHelpers, } from "@yext/studio-plugin";
import { useCallback } from "react";
import { ReactComponent as EditModuleIcon } from "../../icons/editmodule.svg";
import useStudioStore from "../../store/useStudioStore";
import ActionIconWrapper from "./ActionIconWrapper";
export default function EditModuleButton({ state, }) {
    const [setModuleUUIDBeingEdited, setActiveComponentUUID] = useStudioStore((store) => [
        store.pages.setModuleUUIDBeingEdited,
        store.pages.setActiveComponentUUID,
    ]);
    const handleClick = useCallback(() => {
        setActiveComponentUUID(undefined);
        setModuleUUIDBeingEdited(state.uuid);
    }, [state.uuid, setModuleUUIDBeingEdited, setActiveComponentUUID]);
    const moduleName = ComponentStateHelpers.extractRepeatedState(state).componentName;
    return (_jsx("button", { onClick: handleClick, "aria-label": `Edit Module ${moduleName}`, children: _jsx(ActionIconWrapper, { tooltip: "Edit", children: _jsx(EditModuleIcon, {}) }) }));
}
//# sourceMappingURL=EditModuleButton.js.map