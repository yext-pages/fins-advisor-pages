import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import Modal from "./common/Modal";
import useStudioStore from "../store/useStudioStore";
import { ReactComponent as X } from "../icons/x.svg";
import { useCallback } from "react";
import ButtonWithModal from "./common/ButtonWithModal";
/**
 * Renders a button for remove pages from the store. When the button is
 * clicked, a modal is displayed prompting the user to confirm the action.
 */
export default function RemovePageButton({ pageName, }) {
    const removePage = useStudioStore((store) => store.pages.removePage);
    const renderModal = useCallback((isOpen, handleClose) => {
        async function handleConfirm() {
            removePage(pageName);
            await handleClose();
        }
        return (_jsx(Modal, { isOpen: isOpen, title: "Delete Page", body: _jsxs("div", { children: ["Are you sure you want to remove page \"", pageName, "\"?"] }), confirmButtonText: "Delete", handleClose: handleClose, handleConfirm: handleConfirm }));
    }, [pageName, removePage]);
    return (_jsx(ButtonWithModal, { buttonContent: _jsx(X, {}), renderModal: renderModal, ariaLabel: "Remove Page" }));
}
//# sourceMappingURL=RemovePageButton.js.map