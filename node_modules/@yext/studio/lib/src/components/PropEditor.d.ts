/// <reference types="react" />
import { NestedPropType, PropMetadata, PropVal, PropValueKind } from "@yext/studio-plugin";
interface PropEditorProps {
    propName: string;
    propMetadata: Exclude<PropMetadata, NestedPropType>;
    propValue?: string | number | boolean;
    propKind: PropValueKind;
    onPropChange: (propName: string, propVal: PropVal) => void;
    isNested?: boolean;
}
/**
 * Renders an input editor for a single prop of a component or module.
 */
export default function PropEditor({ propName, propMetadata, propValue, propKind, onPropChange, isNested, }: PropEditorProps): JSX.Element;
export declare function renderBranchUI(isNested?: boolean): false | JSX.Element | undefined;
export {};
//# sourceMappingURL=PropEditor.d.ts.map