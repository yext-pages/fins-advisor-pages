import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo } from "react";
import { v4 } from "uuid";
import ErrorBoundary from "./common/ErrorBoundary";
import { Tooltip } from "react-tooltip";
export default function ErrorComponentPreview(props) {
    const { errorComponentState, element } = props;
    const uniqueId = useMemo(() => v4(), []);
    // We cannot use the uuid on ErrorComponentState due to
    // repeaters re-using the same uuid
    const anchorId = `ErrorComponentState-${uniqueId}`;
    return (_jsxs("div", { id: anchorId, className: "hover:shadow-sm border hover:border-red-300 border-transparent", children: [_jsx(ErrorBoundary, { customError: errorComponentState.message, children: element }), _jsx(Tooltip, { content: errorComponentState.message, anchorId: anchorId, className: "text-xs z-20" })] }));
}
//# sourceMappingURL=ErrorComponentPreview.js.map