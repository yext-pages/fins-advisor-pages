/// <reference types="react" />
interface ModalProps {
    isOpen: boolean;
    title: string;
    body: JSX.Element;
    errorMessage?: string;
    handleClose: () => void | Promise<void>;
    handleConfirm: () => void | Promise<void>;
    isConfirmButtonDisabled?: boolean;
    confirmButtonText?: string;
    confirmButtonEnabledColor?: string;
}
export default function Modal({ isOpen, title, body, errorMessage, handleClose, handleConfirm, isConfirmButtonDisabled, confirmButtonText, confirmButtonEnabledColor, }: ModalProps): JSX.Element;
export {};
//# sourceMappingURL=Modal.d.ts.map