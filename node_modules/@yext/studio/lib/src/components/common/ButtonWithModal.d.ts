import { ReactElement } from "react";
import Modal from "./Modal";
import FormModal from "./FormModal";
type ModalType = typeof Modal | typeof FormModal;
export type renderModalFunction = (isOpen: boolean, handleClose: () => void | Promise<void>) => ReactElement<Parameters<ModalType>, ModalType>;
interface ButtonWithModalProps {
    buttonContent: JSX.Element | string;
    renderModal: renderModalFunction;
    buttonClassName?: string;
    ariaLabel?: string;
    disabled?: boolean;
}
export default function ButtonWithModal({ buttonContent, renderModal, buttonClassName, ariaLabel, disabled, }: ButtonWithModalProps): JSX.Element;
export {};
//# sourceMappingURL=ButtonWithModal.d.ts.map