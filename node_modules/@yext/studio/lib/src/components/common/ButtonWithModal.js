import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useCallback, useState } from "react";
export default function ButtonWithModal({ buttonContent, renderModal, buttonClassName, ariaLabel, disabled = false, }) {
    const [showModal, setShowModal] = useState(false);
    const handleButtonClick = useCallback(() => {
        setShowModal(true);
    }, [setShowModal]);
    const handleModalClose = useCallback(() => {
        setShowModal(false);
    }, [setShowModal]);
    return (_jsxs(_Fragment, { children: [_jsx("button", { onClick: handleButtonClick, className: buttonClassName, "aria-label": ariaLabel, disabled: disabled, children: buttonContent }), renderModal(showModal, handleModalClose)] }));
}
//# sourceMappingURL=ButtonWithModal.js.map