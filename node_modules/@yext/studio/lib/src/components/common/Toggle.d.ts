import { ChangeEvent } from "react";
interface ToggleProps {
    checked?: boolean;
    onToggle?: (e: ChangeEvent<HTMLInputElement>) => void;
    disabled?: boolean;
    id?: string;
}
export default function Toggle({ checked, onToggle, disabled, id, }: ToggleProps): JSX.Element;
export {};
//# sourceMappingURL=Toggle.d.ts.map