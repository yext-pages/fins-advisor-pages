import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from "react";
import ComponentTreePreview from "./ComponentTreePreview";
import useStudioStore from "../store/useStudioStore";
export default function ModulePreview(props) {
    const { expressionSources, previewProps, moduleState } = props;
    const getModuleMetadata = useStudioStore((store) => store.fileMetadatas.getModuleMetadata);
    const componentTree = getModuleMetadata(moduleState.metadataUUID).componentTree;
    const moduleExpressionSources = useMemo(() => ({
        ...expressionSources,
        props: previewProps,
    }), [expressionSources, previewProps]);
    return (_jsx(ComponentTreePreview, { componentTree: componentTree, expressionSources: moduleExpressionSources, renderHighlightingContainer: false }));
}
//# sourceMappingURL=ModulePreview.js.map