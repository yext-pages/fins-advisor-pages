import { jsx as _jsx } from "react/jsx-runtime";
import { useMemo, useState } from "react";
import AddPageContext from "./AddPageContext";
const initialPageData = {
    isStatic: true,
};
export default function AddPageContextProvider(props) {
    const [state, setState] = useState(initialPageData);
    const contextValue = useMemo(() => ({
        state,
        actions: {
            setIsStatic: (isStatic) => setState({ ...state, isStatic }),
            setStreamScope: (streamScope) => setState({ ...state, streamScope }),
            resetState: () => setState(initialPageData),
        },
    }), [state]);
    return (_jsx(AddPageContext.Provider, { value: contextValue, children: props.children }));
}
//# sourceMappingURL=AddPageContextProvider.js.map