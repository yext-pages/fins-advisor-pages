import { jsx as _jsx } from "react/jsx-runtime";
import useStudioStore from "../store/useStudioStore";
import { ReactComponent as X } from "../icons/x.svg";
import { useCallback } from "react";
/**
 * Renders a button for removing an element from the component tree of the
 * active page.
 */
export default function RemoveElementButton({ elementUUID, }) {
    const removeComponent = useStudioStore((store) => store.actions.removeComponent);
    const handleClick = useCallback(() => {
        removeComponent(elementUUID);
    }, [elementUUID, removeComponent]);
    return (_jsx("button", { onClick: handleClick, "aria-label": "Remove Element", children: _jsx(X, {}) }));
}
//# sourceMappingURL=RemoveElementButton.js.map