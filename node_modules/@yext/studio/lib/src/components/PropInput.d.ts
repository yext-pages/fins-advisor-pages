/// <reference types="react" />
import { PropType, PropValueKind } from "@yext/studio-plugin";
interface PropInputProps<T = string | number | boolean> {
    propType: PropType;
    propValue?: T;
    onChange: (value: T) => void;
    unionValues?: string[];
    propKind: PropValueKind;
}
/**
 * Renders the input element of a PropEditor component, that
 * will update the corresponding prop's value for the active
 * component based on user's input.
 */
export default function PropInput({ propType, propValue, onChange, unionValues, propKind, }: PropInputProps): JSX.Element;
export {};
//# sourceMappingURL=PropInput.d.ts.map