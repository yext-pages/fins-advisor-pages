import { FileMetadata, PageState } from "@yext/studio-plugin";
/**
 * Iterates through a record of objects that contain a componentTree,
 * and removes all top level fragments.
 */
export default function removeTopLevelFragments<T extends PageState | FileMetadata>(record: Record<string, T>): Record<string, T>;
//# sourceMappingURL=removeTopLevelFragments.d.ts.map