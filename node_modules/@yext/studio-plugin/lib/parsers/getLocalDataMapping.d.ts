/**
 * Import the user's localData/mapping.json file, so that studio can find the user's test data.
 */
export default function getLocalDataMapping(localDataPath: string): Promise<Record<string, string[]>>;
//# sourceMappingURL=getLocalDataMapping.d.ts.map