/**
 * Calculates the import needed to import a file into another;
 *
 * Assumes given paths are absolute.
 */
export default function getImportSpecifier(baseFilePath: string, filePathToBeImported: string): string;
//# sourceMappingURL=getImportSpecifier.d.ts.map