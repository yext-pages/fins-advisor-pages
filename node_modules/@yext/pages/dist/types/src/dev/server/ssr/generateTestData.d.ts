import { FeaturesConfig } from "../../../common/src/feature/features.js";
import { ProjectStructure } from "../../../common/src/project/structure.js";
import { ViteDevServer } from "vite";
/**
 * generateTestData will run yext pages generate-test-data and return true in
 * the event of a successful run and false in the event of a failure.
 *
 * @param hostname The hostname of the site
 * @returns a boolean on whether test data generation was successful
 */
export declare const generateTestData: (hostname?: string) => Promise<boolean>;
export declare const generateTestDataForSlug: (stdout: NodeJS.WriteStream, vite: ViteDevServer, slug: string, locale: string, projectStructure: ProjectStructure) => Promise<any>;
export declare const generateTestDataForPage: (stdout: NodeJS.WriteStream, featuresConfig: FeaturesConfig, entityId: string, locale: string, projectStructure: ProjectStructure) => Promise<any>;
