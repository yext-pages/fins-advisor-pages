import { TemplateModuleInternal } from "../../../common/src/template/internal/types.js";
import { Response } from "express-serve-static-core";
import { ViteDevServer } from "vite";
/**
 * Renders the HTML for a static {@link TemplateModuleInternal},
 * and sends it back to the Response.
 */
export default function sendStaticPage(res: Response, vite: ViteDevServer, matchingStaticTemplate: TemplateModuleInternal<any, any>, locale: string, templatePath: string): Promise<void>;
