import { ViteDevServer } from "vite";
/**
 * LocalDataManifest contains data parsed out from the filesystem that is then used
 * to generate in the indexPage.
 */
export interface LocalDataManifest {
    static: {
        featureName: string;
        staticURL: string;
    }[];
    entity: Map<string, {
        entityId: string;
        slug: string | undefined;
    }[]>;
}
export declare const getLocalDataManifest: (vite: ViteDevServer, templateFilepaths: string[]) => Promise<LocalDataManifest>;
export declare const getLocalDataForEntityOrStaticPage: ({ locale, featureName, entityId, }: {
    locale: string;
    featureName: string;
    entityId: string;
}) => Promise<Record<string, any>>;
export declare const getLocalDataForSlug: ({ locale, slug, }: {
    locale: string;
    slug: string;
}) => Promise<Record<string, any>>;
