/// <reference types="react" />
import { LocationMapProps } from "./types.js";
export declare const LocationMap: {
    ({ children, coordinate, linkSameTab, pinUrl, onClick, onHover, onFocus, ...mapProps }: LocationMapProps): JSX.Element;
    defaultProps: {
        controls: boolean;
        panHandler: () => null;
        provider: any;
        singleZoom: number;
    };
};
