import React from "react";
import { Map as MapType } from "@yext/components-tsx-maps";
import type { MapProps, MapContextType } from "./types.js";
export declare const MapContext: React.Context<MapContextType | null>;
export declare function useMapContext(): MapType;
export declare const Map: {
    ({ apiKey, bounds, children, className, clientKey, controls, defaultCenter, defaultZoom, mapRef, padding, panStartHandler, panHandler, provider, providerOptions, singleZoom, }: MapProps): JSX.Element;
    defaultProps: {
        controls: boolean;
        defaultCenter: {
            latitude: number;
            longitude: number;
        };
        defaultZoom: number;
        padding: {
            bottom: number;
            left: number;
            right: number;
            top: number;
        };
        panHandler: () => null;
        panStartHandler: () => null;
        provider: any;
        singleZoom: number;
    };
};
