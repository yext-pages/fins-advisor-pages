import glob from "glob";
import path from "path";
const getTemplateFilepaths = (paths) => {
  const templateFilepaths = [];
  const addedFilenames = /* @__PURE__ */ new Set();
  paths.forEach((p) => {
    const filepaths = glob.sync(`${p.getAbsolutePath()}/*.{tsx,jsx,js,ts}`);
    filepaths.forEach((f) => {
      const fileName = path.basename(f);
      if (!addedFilenames.has(fileName)) {
        addedFilenames.add(fileName);
        templateFilepaths.push(f);
      }
    });
  });
  return templateFilepaths;
};
const getTemplateFilepathsFromProjectStructure = (projectStructure) => {
  return getTemplateFilepaths(
    projectStructure.scopedTemplatesPath ? [projectStructure.scopedTemplatesPath, projectStructure.templatesRoot] : [projectStructure.templatesRoot]
  );
};
export {
  getTemplateFilepaths,
  getTemplateFilepathsFromProjectStructure
};
